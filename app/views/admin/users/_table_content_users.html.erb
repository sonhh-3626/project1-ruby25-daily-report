<% if @users.empty? %>
  <tr>
    <td colspan="6" class="text-center">
      <%= t "users.index.no_users" %>
    </td>
  </tr>
<% else %>
  <table class="table table-striped">
    <thead>
      <tr>
        <th><i class="fas fa-hashtag me-1"></i> <%= t "index.table.order" %></th>
        <th><i class="fas fa-user me-1"></i> <%= t "users.index.table.name" %></th>
        <th><i class="fas fa-envelope me-1"></i> <%= t "users.index.table.email" %></th>
        <th><i class="fas fa-building me-1"></i> <%= t "users.index.table.department" %></th>
        <th><i class="fas fa-user-tag me-1"></i> <%= t "users.index.table.role" %></th>
        <th><i class="fas fa-toggle-on me-1"></i> <%= t "users.index.table.active" %></th>
        <th><i class="fas fa-cogs me-1"></i> <%= t "users.index.table.actions" %></th>
      </tr>
    </thead>
    <tbody>
      <%= render partial: "user", collection: @users, as: :user, locals: {pagy: @pagy} %>
    </tbody>
  </table>

  <div class="d-flex text-center mt-4">
    <%== pagy_bootstrap_nav(@pagy) if @pagy.pages > 1 %>
  </div>
<% end %>
